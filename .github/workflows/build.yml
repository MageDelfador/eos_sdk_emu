name: Build Windows (Static Single DLL)

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install pefile
        run: pip install pefile

      - name: Setup vcpkg
        shell: pwsh
        run: |
          $vcpkgRoot = "D:\vcpkg"
          if (-not (Test-Path $vcpkgRoot)) {
            git clone https://github.com/microsoft/vcpkg.git $vcpkgRoot
          }
          & "$vcpkgRoot\bootstrap-vcpkg.bat"
          echo "VCPKG_ROOT=$vcpkgRoot" | Out-File -FilePath $env:GITHUB_ENV -Append
          echo "$vcpkgRoot" | Out-File -FilePath $env:GITHUB_PATH -Append

      - name: Setup MSVC environment
        uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: x64

      - name: Install dependencies
        shell: pwsh
        run: |
          $installedDir = "D:\vcpkg\installed\x64-windows-static"
          if (Test-Path $installedDir) {
            Remove-Item -Recurse -Force $installedDir
          }
          vcpkg install protobuf:x64-windows-static nlohmann-json:x64-windows-static --recurse --binarysource=clear
          vcpkg list

      - name: Configure CMake
        shell: pwsh
        run: |
          $buildDir = "$env:GITHUB_WORKSPACE\build"
          if (Test-Path $buildDir) {
            Remove-Item -Recurse -Force $buildDir
          }
          New-Item -ItemType Directory -Path $buildDir | Out-Null

          cmake -S "$env:GITHUB_WORKSPACE" `
                -B $buildDir `
                -G "Ninja" `
                -DCMAKE_BUILD_TYPE=Release `
                -DX64=ON `
                "-DCMAKE_TOOLCHAIN_FILE=D:/vcpkg/scripts/buildsystems/vcpkg.cmake" `
                "-DVCPKG_TARGET_TRIPLET=x64-windows-static" `
                "-DCMAKE_PREFIX_PATH=D:/vcpkg/installed/x64-windows-static"

      - name: Build
        shell: pwsh
        run: |
          cmake --build "$env:GITHUB_WORKSPACE\build"

      - name: Verify DLL imports
        shell: pwsh
        run: |
          $dll = Get-ChildItem -Path "$env:GITHUB_WORKSPACE" -Recurse -Filter "EOSSDK-Win64-Shipping.dll" | Select-Object -First 1
          if (-not $dll) {
            Write-Error "EOSSDK-Win64-Shipping.dll not found anywhere in workspace!"
            exit 1
          }
          Write-Host "Found DLL at: $($dll.FullName)"
          python -c "import pefile; pe=pefile.PE(r'$($dll.FullName)'); deps=[e.dll.decode() for e in pe.DIRECTORY_ENTRY_IMPORT]; print('IMPORTS:', ', '.join(sorted(deps)))"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: EOSSDK-Win64-Shipping
          path: |
            **/EOSSDK-Win64-Shipping.dll
            **/*.dll